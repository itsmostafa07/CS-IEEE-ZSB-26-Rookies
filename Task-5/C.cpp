#include <bits/stdc++.h>
#define ll long long
#define lli long long int
#define vi vector<int>
#define vll vector<long long>
using namespace std;

// for gcd:
// a = b . q * r where q is how many times b appears in a, and r is reminder of it

void solve() {
    int n;
    cin >> n;
    cout << n / 2 << endl;
}

int main() {
    ios_base::sync_with_stdio(false); 
    cin.tie(NULL); 
    cout.tie(NULL);

    int t; cin >> t;
    while(t--) {
        solve();
    }
}
